import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.emilycogsdill.workout',
  appName: 'Workout Tracker',
  webDir: 'dist'
};

export default config;
